FROM mailhog/mailhog

ENV PORT 25
ENV PORT_UI 80

EXPOSE $PORT $PORT_UI
USER root

ENV MH_API_BIND_ADDR 0.0.0.0:$PORT_UI
ENV MH_UI_BIND_ADDR 0.0.0.0:$PORT_UI
ENV MH_SMTP_BIND_ADDR 0.0.0.0:$PORT

ENTRYPOINT ["MailHog"]
